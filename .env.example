# Copy to .env and fill in real values. Never commit .env.

# Google Gemini (Narrator)
GEMINI_API_KEY=your_key_here

# Local node hostnames or IPs (defaults assume LAN DNS; adjust if needed)
ALIEN_HOST=alien
ALIEN_PORT=8080
SARDINIA_HOST=sardinia
SARDINIA_PORT=1234
